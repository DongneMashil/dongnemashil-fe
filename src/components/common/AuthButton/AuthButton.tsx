import React from 'react';
import { StAuthButton, StAuthButtonProps } from './AuthButton.styles';

//? interface 합치기? StAuthButtonProps + ButtonProps

export const AuthButton = () => {
  return <StAuthButton>AuthButton</StAuthButton>;
};
